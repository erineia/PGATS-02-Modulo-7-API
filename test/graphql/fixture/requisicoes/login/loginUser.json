{
   "query": "mutation Mutation($username: String!, $password: String!) {loginUser(username: $username, password: $password) {token}}",
        "variables": {
             "username": "Neia",
             "password": "123456"
        }
}
